# Server Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Database Configuration
# Supabase PostgreSQL connection string
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.kelcuxyiayqbdherncrm.supabase.co:5432/postgres

# CORS Configuration
# Frontend URL for CORS
CORS_ORIGIN=http://localhost:5138

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Oracle Service Configuration
# Cron schedule for price updates (default: every 2 minutes)
# Format: minute hour day month day-of-week
# Examples:
#   */2 * * * * - Every 2 minutes
#   */3 * * * * - Every 3 minutes
#   0 */1 * * * - Every hour
ORACLE_CRON_SCHEDULE=*/2 * * * *

# Price change threshold for updates (in percentage)
# Only update if price changes by more than this percentage
# Default: 0.05 (0.05%)
ORACLE_PRICE_THRESHOLD=0.05

# Timezone for cron scheduling
ORACLE_TIMEZONE=America/New_York

# Algorand Network Configuration
# Network to connect to (mainnet, testnet, or betanet)
ALGORAND_NETWORK=testnet

# Oracle application ID on Algorand
ORACLE_APP_ID=

# Oracle service account mnemonic (for signing update transactions)
# Keep this secret and secure!
ORACLE_ACCOUNT_MNEMONIC=

